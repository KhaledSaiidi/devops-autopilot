apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: ${ALB_CONTROLLER_NAMESPACE}
resources:
  - namespace.yaml
helmCharts:
  - name: placeholder
    repo: file://../.charts/placeholder
    releaseName: ingress-placeholder
    namespace: ${ALB_CONTROLLER_NAMESPACE}
    valuesFile: values.yaml
